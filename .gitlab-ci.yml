image: ubuntu
before_script:
  # Setup SSH deploy keys
   - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client git -y )'
   - eval $(ssh-agent -s)
   - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
   - mkdir -p ~/.ssh
   - chmod 700 ~/.ssh
   - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'
stages:
    - test
#    - build

server_ssh:
     stage: test
     environment:
          name: staging
          url: ec2-34-213-177-214.us-west-2.compute.amazonaws.com
     script:
        - ssh ubuntu@ec2-34-213-177-214.us-west-2.compute.amazonaws.com "cd jishoy/ ; rm -r *; git clone https://gouthz:Gouthz@123@github.com/gouthz/farmers-app.git" 
        - pwd
        - cd  
        
