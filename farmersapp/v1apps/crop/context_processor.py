# from .models import BuyRequest
#
#
# def access_banner_image(request):
#     """
#       The context processor must return a dictionary.
#     """
#     buy_requests = BuyRequest.objects.filter(approve=False).count()
#     return {'buy_requests':buy_requests}